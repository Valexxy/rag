name: sovereign-ai-backend
type: web
image: null
git:
  repository: github.com/Valexxy/rag
  branch: main
build:
  command: pip install -r requirements.txt
run:
  command: uvicorn main:app --host 0.0.0.0 --port $PORT
env:
  - key: PORT
    value: "8000"
